// Write your solution in this file!
// Declare customerName in the global scope
var customerName = "bob";

// Create a function to return customerName
function returnCustomerName() {
  return customerName;
}
// Declare customerName in the global scope
var customerName = "bob";

// Create a function to modify customerName to uppercase
function upperCaseCustomerName() {
  customerName = customerName.toUpperCase();
}
// Declare the bestCustomer variable in the global scope
var bestCustomer;

// Declare the bestCustomer variable in the global scope
var bestCustomer;

// Create a function to set the bestCustomer variable
function setBestCustomer() {
  bestCustomer = "John";
}

// Declare the bestCustomer variable in the global scope
var bestCustomer = "Initial Customer";
